{% extends 'base.html.twig' %}

{% block title %}Edit Quiz{% endblock %}

{% block body %}
    <h1>Edit Quiz</h1>

    {{ include('quiz/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="btns">
    <a class="edit" href="{{ path('app_question_new') }}">add a question</a>
    </div>
<div class="btns">
    <a class="btn" href="{{ path('app_quiz_index') }}">back to list</a>
</div>
    {{ include('quiz/_delete_form.html.twig') }}
{% endblock %}
