{% extends 'base.html.twig' %}

{% block title %}Quizzes{% endblock %}

{% block body %}
    <div class="grid-container">
    <div>
    <h1> {{ quiz.name}} </h1>
    <div class="readMe">
        <p class="bodyText">
            Les gaz à effet de serre sont des gaz présents dans l'atmosphère qui agissent comme une couverture autour de la Terre, piégeant la chaleur qui rayonne
            de la surface de la Terre et l'empêchant de s'échapper dans l'espace. </p>
    </div>
    <img class="cartoon" src="{{ asset('images/working.png') }}" alt="child thinking"/>
    <div class="readMe">
        <p class="bodyText"> C'est ce que l'on appelle l'effet de serre, et c'est ce qui permet à la Terre de rester suffisamment chaude pour permettre la vie.
            Sans les gaz à effet de serre, la Terre serait trop froide la nuit pour que les plantes puissent pousser ou que les animaux puissent survivre.</p>
    </div>
    <h3>Vrai ou Faux ?</h3>
    <div class="question">
    {% for question in quiz.questions %}
        {{ question.question }}
    </div>
    <div>
        {{ form_start(saForm) }}
        {{ form_row(saForm.answer, {
            attr: { placeholder: "answer", class: 'placeholder' }
        }) }}
        <div class="btns">
        <button class="btn"> Submit </button>
        </div>
        {{ form_end(saForm) }}
    </div>
    {% for message in app.flashes('correct') %}
        <div class="correct">
          {#   {{ message }} #}
            <img class="cartoon" src="{{ asset('images/tresBien.png') }}" alt="tres bien !"/>
        </div>
    {%  endfor %}
        {% for message in app.flashes('oops') %}
        <div class="oops">
          {#   {{ message }} #}
            <img class="cartoon" src="{{ asset('images/tryAgain.png') }}" alt="try again !"/>
        </div>
    {%  endfor %}
    {%  endfor %}
    <div>
{% endblock %}